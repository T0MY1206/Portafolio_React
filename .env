# URL del servidor que registra visitas (opcional).
# Si no se define, la app no envía datos de visita.
# Para desarrollo local: npm run server (puerto 3001) y definir:
VITE_VISITS_API_URL=http://localhost:3001